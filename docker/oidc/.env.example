# Papermerge OIDC Docker Compose Environment Variables
# Copy this file to .env and set the values

# OAuth2 Proxy cookie secret (REQUIRED)
# Generate with: openssl rand -base64 32
OAUTH2_COOKIE_SECRET=
